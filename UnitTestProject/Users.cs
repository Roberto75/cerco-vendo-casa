using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace UnitTestProject
{
    [TestClass]
    public class Users
    {
        [TestMethod]
        public void login()
        {
            // MyUsers.UserManager manager = new MyUsers.UserManager();
            //manager.u
            //MyUsers u = new MyUsers((;))

            long userId = -1;
            userId = 3;
            // logica
            if (userId == -1)
            {
                Assert.Fail();
            }
        }
    }
}
